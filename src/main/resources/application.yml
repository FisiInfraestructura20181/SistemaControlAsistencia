spring:
  datasource:
    url: jdbc:mysql://db4free.net:3306/asistencia?verifyServerCertificate=false&useSSL=false&requireSSL=false
    username: fisi_infra
    password: fi$iinfr@
  mvc:
    favicon:
      enabled: true
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
